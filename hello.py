# import logging
#
# from vehicle import Car, Cylindre, Shatun, Calenval, Engine, Wheel, Vkladish
#
# console_handler = logging.StreamHandler()
# console_handler.setLevel(logging.WARNING)
#
# file_handler = logging.FileHandler('file_logger.log')
#
#
# logging.basicConfig(handlers=[console_handler, file_handler],
#                     format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
#                     level=logging.NOTSET)
#
# cylinders = [Cylindre(5), Cylindre(5), Cylindre(5)]
# shatuns = [Shatun(Vkladish(), 5), Shatun(Vkladish(), 5)]
# calenval = Calenval()
# engine = Engine(cylinders, shatuns, calenval)
# wheels = [Wheel(17), Wheel(17), Wheel(17), Wheel(17)]
#
# car = Car(engine, wheels, fuel_volume=10)
# car.start()
#
# car.fuel_volume = 0
# car.start()
import time

a = 10
int()
str
print(int)
